# Assignment_1

Q1?  create a valid xml for school results
  also define its schema and validate with schema

  Q2? what are all tags of html old
        why div and span exists?
     what is box model?
 craate a html page to render school results using above xml data?

Q3?
 diffrence between TCP/IP and UDP. cite some xamples of TCP issues and adavantges , same for UDP.
  WHY Http is based on TCP?

  domain name? and an ip addresss?
  DNS? what are orders try editing hosts file?

  host

  port number of a server on op
  what are standard port numbers of various internet services? smtp(email), SSH, http, https

  create a local server with random port changes?  spring boot service

  create apis using all http methods in spring boot
         create a file that contains school results
          get api to get result of one student and all students
          post to add a result to that file?
          delete to delete a result

           try put etc also


what is json?

 create  sample json for student results and use same for spring apis that you create above

  read about all common http status services?
  read and write about all common http headers experiment with various websites

  use postman
  what are cookies experiment with them

  experiment with rest complian apis


 #write a code to hit your own apis and parse them using server code
